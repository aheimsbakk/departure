---
when: 2026-03-13T21:48:56Z
why: Close the two last open ANALYZE.md issues — SRP violation in ui.js and memory leak in gps-dropdown.js
what: Extract footer into footer.js (§8/§13) and switch gps-dropdown to event delegation (§11)
model: github-copilot/claude-sonnet-4.6
tags: [refactor, srp, memory, gps-dropdown, footer, analyze]
---

Extracted `createFooter()` and `updateFooterTranslations()` from `src/ui/ui.js` into the new `src/ui/footer.js` module, resolving the SRP/anti-monolith violation (ANALYZE.md #9). Switched `gps-dropdown.js` from per-item click listeners to a single delegated `click` listener on the `menu` element backed by a `Map`, so `innerHTML = ''` no longer leaves dangling closures (ANALYZE.md #10). Updated `tests/footer-link.test.mjs` to point at the new file; all tests pass. All 20 ANALYZE.md issues are now resolved. Bumped to v1.38.5.
