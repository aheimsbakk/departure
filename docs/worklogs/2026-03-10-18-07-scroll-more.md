---
when: 2026-03-10T18:07:45Z
why: Users needed a way to see more departures beyond the configured limit without changing settings
what: Add scroll-for-more Fibonacci departure loader with pull gesture, resistance, and max-hint
model: github-copilot/claude-sonnet-4.6
tags: [feature, ux, scroll, departures, i18n, pwa]
---

Implemented a scroll-for-more interaction on the departure board that progressively loads more departures following the Fibonacci sequence (1→2→3→5→8→13→21). New files: `src/app/scroll-more.js` (core module with touch/wheel/mouse drag support, rubber-band resistance, marginTop displacement, bounce-back animation) and `src/css/scroll-more.css` (bounce indicator styles). Modified `src/app/fetch-loop.js` to support a runtime `numDeparturesOverride`, `src/app/handlers.js` to reset the override on station/settings change, `src/app.js` to wire up the module, `src/style.css` and `src/sw.js` to register the new assets, and `src/i18n/translations.js` to add `scrollForMore`/`scrollMaxReached` keys across all 12 languages. Bumped version to 1.37.0.
