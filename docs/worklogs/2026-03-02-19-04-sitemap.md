---
when: 2026-03-02T19:04:35Z
why: add sitemap.xml for SEO and establish rule for keeping it current
what: add src/sitemap.xml and PROJECT_RULES.md sitemap lastmod rule
model: github-copilot/claude-sonnet-4.6
tags: [seo, sitemap, docs]
---

Added `src/sitemap.xml` with a single URL entry and `<lastmod>` only (no ignored `<changefreq>`/`<priority>` attributes). Populated `docs/PROJECT_RULES.md` with a rule requiring `<lastmod>` to be updated on every release that touches `src/`. Version bumped to 1.34.10.
